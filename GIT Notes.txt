Day 0
Git is a software to install in laptop.
Git Bash is a central repo to which code is pushed in 3 steps .
Install GIT Bash.
Linux

pwd --- present working dorectory

Touch file1 file2 file3 ---- to create file through commands
mkdir folder1 folder2   ---- to create folders/directory
rm f1 f2                -- to remove.delete file
rmdir folder2           ---- to remove directory
commandds to view in linux
ls       --regular
ll  --detailed with access 
ls -lrth --detailed
la -la  ---hidden files included/.
la  -lart -- detailed with hidden!?

cd dir name - to goinside directory
cd .. -- to come out of that directory --one step back
cat f2 -- to show content in files
history to check the total list of commands we practiced.
----------------------------------------------------------------------------
----------------------------------------------------------------------------
Day1

git init -- intalize empty repo

code will move to git in 3 steps.
1. workspace  -- contains code /files by develope
2. staggingindex ---  git add fnmae  moving 
3. indexing  -- git commit 
3. local repo git push is moved to repo. we need to check the status
git status -- will give if any files there to add/ difference between worksapce and local repo
git log -- will have all the commit ID's  
git show commiiID -- this will give what happend in that commitiD
git config --global user.name "geet"
git config --global user.email "geethikapatro@28@gmail.com"

we can add username,email etc with the above 

---------------------------------------------------------------
----------------------------------------------------------------
Day2

GitHub repo created 
pushed file1 file2 from pc to git repo.
in the intial push, it ask for gmail,password then choose email-password /pat token(git setting->deveoper settings->token classic->gice access and remove /keeep exipry and PAT created save it somewhere and use this to while trying to push).
In our pc, our git bub mail and password will be saved in credentail manager  after 1st push. every time it will ask for the cred, if asked please provide above cred created.

---------------------------------------------------------------
----------------------------------------------------------------
Day3
till today we worked on our individual repo. today we push code  to devopstraininghub/b18projectrepo. 
for that we need to have collaborations access. Settings->collaborations->add the mail
git pull -rebase -- when merge conflict occure, like if geethika and patro are workig on same repo simulatinously. if we use rebase from geethika account, patro file will come to geethika local and  now we can pull from git.
git branch 
branch is a copy of data. developer will take copy of main in a branch do the required changes and test in that branch , once ready will create release branch and push it main and production.for defects post production will have a hotfix branch.

git branch <branchname>   -- to create branch
git init -- intail commit 
touch test -- sample file
git checkout/git switch <branchname>  -- to switch to branch
git merge <branchname>    -- to merge
or 
git checkout -b <branchname> --- directly checkouts current branch code and creates a new branch
for local deletion
git branch -d <branchname>  -- regular delete
git branch -D <branchname>  --- force delete

To delete in remote 
git push origin -d/--delete <branchname>


---------------------------------------------------------------
---------------------------------------------------------------

Day4
branching strategy -- a main branch and take a cut from it ,create a branch work on the changes then raise PR(Pull Request/Merge Request) to main. release and hofix branches are created as necessary.

echo 'content to be added'  -- echo is to display text.
echo 'adding data to file' > filename -- to add data to file.
cat filename -- wil give the data inside file on console.
when given git log. if head->banch is the branch ahead of other main branch

to send/push the branch created or data to the creted branch ,.command .
git push origin/gitURL branchnameeg: git push origin windowsbranch
git cherry-pick commitID(get the commitID from git log command). 

if we use merge , it merge everything to branch. but if we need to merge specific commitID then we need to use cherry-pick

for local deletion
git branch -d <branchname>  -- regular delete
git branch -D <branchname>  --- force delete

To delete in remote 
git push origin -d/--delete <branchname>

git checkout geethika 
git checkout geethika --   if there is a file and branch and git confused then use this command.

donot practice delete onmain . so create geethika and geethika-feature branch and practice 
=============================================================
=============================================================
Day5 

we can use 2 commands at once.like git add . && git commit -m 'two commands at once'
centrally deletion/ adding  we should us epush only. 


VI Editor

vim/vi/nano

vim fname
pres esc --- I (insert mode)

--add data ---
esc shift: w
esc shift: q
esc shift: wq


git config --list to check the current config of git.email id present here will be used to push /pull etc..go if want to switch then use config command and witch the email.
-------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------
Day 6

if we are paralleling working on branch and try to merge them , then vi editor open and ask for commit message once we save and exit esc shift:wq two commitit will be seen in git log stating one for each branch.

ig branch main and feature branch has file xyz and  xyz have different data/code in these files.when tryind to merge these two branches then merge conflict will arise. and can be seen in file with vi editor as <<<<<<< HEAD
iam conflict current branch data

iam dumbali other branch data
 developer will decide and keep part of data required on the file and add  nd commit . now conflict resolved and merged.

filter commands in git to ease our work
git log -5 [5 is no of previous noof logs]
git log --oneline [id in short form and commit message]
git log -- author "geethika" -- work done by geethika will come
git log -- since "yyyy-mm--dd"
we can combine these all also as git log --oneline -5 --author 
git commit --amend -m 'changes commit message '    --to change/modify latest commit message only 
we can also have it as  git commit --amend -m 'changes commit message '  -m 'jiraID'
we can also changes made with latest/previous commit with above command.
if checked git log you can see old commitid wil be replaces=d with new one.

alias-- simply shortcut  no realtime usuage actually

git config --list
git config --global alias.newlogname log
git config --global alias.s status 

to revert the alias changes 
git config --global --unset alias.s   it will go back to git status 

a new file created f1 in workspace should be stagged 1st i.e git add , 
if changes made in  f1 can be moved to indexing commit the command for that is 
git commit -am 'commit messageqo'
<<<<<<< HEAD
=======
>>>>>>> 371b116
-------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------
Day 7

GIT TAG
after acheiving a milestone like eg audiocall code completed,video call code comeplted.we can label that ads tags in git.
this is similar to branch commands
git tag   -- to see the tags
git tag <tagname> commitID  -- if no commt ID provided it will take latest commitID
git checkout tag
git tag -d <tagname>
git push origin <tagname>
git push origin -d <tagname>

PR Raising 

we create PR in realtime for merging branches. 
go to pull request, create pull choose source and destination . it will show the differnce and what is going to merge , then we can have reviwere/approval from right side max 14 members then.
once approved, we can merge .
Reviere will have a pop up sstating the details . he can approve/ add commeent s etc.. 
then merge pull request then it will be merged.
this PR raising will work if we are part of enterise project where we can contributors.

If we want to contribute to OpenSource project then we need to fork it 
fork cocnepts

if we fork a project we will get a copy of it out git repo. 
once we ge that repo in our rpo , we can add files/do our code .
if we see this usuaeful to the open source project we forked, then click contribute (like a PR)then author of the Opensource will cehcka nd merge/not as per thier need.
===================================================================================================================================

Day8
git branch protection

in real time main always has prod data. so when PR raised shoukd be review by atleat 3 reviews.
got branch settings-> and under branch rule we can write all thes steps. min reviewe required aand other things. also we can do who to force push/ etc also in it.


we push in 2 states from local to remote repo.same way we can traverse back using git reset command.

git reset --head filename  -- from commit to add(stage to worksapce)
git reset --soft old-commitID(n-1) ---  from localrepo to stagging 
git reset --mixed commitID(n-1)   ---- from localrepo to worksapce
git reset --hard commitID(n-1)  -- to delete the data

to temporary store the data in git we use stash commands. eg if dev is working on task1 and proirity task2 need to be worked on, then task1 can be stashed without making the work loss. task1 not comleted soc ant be commited.

touch task1
git add .
git stash save task1 -- moves task1 to stash(temporary file)
git stash list  -- gives stashed list with stashid

to get back the stashed data back to stagging we can do 3 days
git stash apply stashid --like copy paste
git stash pop   stasthid -- like cut paste
git stash drop  stashid --like delete

statshing is done before commit 


GIT MERGE ||GIT REBASE ||

git merge branchname --- creats a commit id and merge 
merges the whole with logs and history
git rebase brnachname --- one branch will sit on another branch
moves branchcommit on top of another branch. no istory is maintanined.

====================================================================================================================================================================================
Day 9
Batch 18 notes repo link ---  https://github.com/devopstraininghub/BATCH18A_B18D_RUNNING_NOTES


Git fetch -- in out pc we have origin so when used fetch the data comes till origin only not to workspace
Try git fetch and then do lll we see it came to origin/main
Then we need to do git merge -- to take it to local workspace.
Instead we mostly use git pull which does both.
Git pull -- git fetch + git merge. --- pull brings the new changes done in remote to local.



GITIGNORE

Vim .gitignore. creates a file name .gitignore
Echo log > .gitignore. add log file to .gitignore file and now it wont be tracked anymore.
Git add . && git commit -m 'ignore file'
Cat .gitignore -- will show the data inside file.
If we want to track the files again remove the filename from .gitignore then we are good to rtrack again.

Consider Friday code worked , but midday something added and failed. We need to check and remove it  . Below are the steps.
To rollback the changes done in a commit id 

Git revert commitID(CID) --- imp feature to revert incase of something goes wrong

Revert creates a new committed and reverts the changes done with a particular commit.


Reset is to commit changes from local to remote
Revert to rollback changes done in a commit ID.
=============================================================================================




