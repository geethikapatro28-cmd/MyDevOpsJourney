

Day 1 
Demo session only explaining wha tis cloud and develops etc.. which cloud to learn etc
===============================================================================================
Day 2 

AWS free tier account creation
validity - 6 mmonths- or 200$
anything with cpu RAM and storage is server. 
go to gogle search grate aws free tier and login once creted.
created a free account needed is a gmailid and fredit cad account to verify.
always use n.vergina as the region and do the work post login.
EC2-- Amazon Elastic Compute Cloud
Amazon EC2 is a core web service provided by Amazon Web Services (AWS) that allows you to rent virtual computers—known as instances—to run your own computer applications.
Go to EC2->launch instance->give linux server as name - choose aws linux-> instance type t3.micro as it is free eligible
create new key-pair
two keys created-- public key injects in awsprivate key downloads to laptop.leave the other settings as is.
then in the network settings- we need to create firewall-? firewall is to allow whoom at which port etc.. linke securing the virtual machine
443 should be open to connect to https  protical sites
port is entry and exit points to server.

firewall ->create new security group give a name ,description.
as currently our is a practice type->all traffic and source type- anywhere choose it. in reality we wil choose certails porta and certain types and etc as per requirement.
group rule  we can add as many as we can and  we can either edit or create new one. click launch instance.

in left panel ->go to instance-> right click on server ->click connect.now it shows through browser or ssh.
it has 2 options though web and sshif web is used we can use there only
If clicked ssh ->it asks for key 
in laptop go to gi
open the git bash in the folder where .pem key of aws is stored.
provide the commands provided in the aws . 
now we are connected to the ec2 through laptop. here the public key(aws one) and private key(downloaded in pc) did handshake and logged in to the server we created.
in gitbash type whoami
gives ec2-user name 
linux server ultimate user is root user
in console  --sudo su
it changes the user to root user.
hostnamectl set-hostname <desired hostname>
hotname -I   -- to check ip
uname   -- to check osname
uname -a   --- to check all config 
Uptime --- tell how much time the server is up
clear

Basic Linux commands
whoami --   ec2 server after connecting server
sudo su -- switch to root user
pwd=present working directory path
view
ls
ll
ls -lart
ls-lrth

touch f1 g2 g3 create file touch
mkdir dir1 dir2
rm -rf f1 f2 --delete file
rm -rf dir1 -- deleted folder
rm -rf * -- to delte all -- never use
cat f1 -- see the content in f1
to go inside folder cd foldername/folderpath
to come one step outside folder (immediate parent)cd ..

===============================================================================================

Day3 

lscpu --Cpu count check 
Free /free -m/ free -g. -- gives ram details

User Profiles

Top user in linux -root user - boss
Admin user -- next top -- can create 
Regular user --  permissions as per required only

We can create these users in linux using below commands

Useradd madhu.  -- command to create user
Passwd madhu.   -- command to create password for user


Sudo su - === logs in to root user
su - maddhu === logs in as madhu user su meaning SwitchUser
/home/<usernmae> ===  path like in windows c/users
/etc/passwd === file has user entry details , so use cat /etc/passwd we can see the content in this file 

This file contains user stanza 

Example :
ec2-user:x:1000:1000:EC2 Default User:/home/ec2-user:/bin/bash
geet:x:1001:1001::/home/geet:/bin/bash

Username :x: userid :usergroupID :comment :user path:were user commands execute

Created feet password :geethika

Here x indicates password free and we can add /not our comments .
Root user's groupdID will be always 0.
User mod -c "devopsuser" made

Cd /home --- gives user folder . Here home is the root folder like C in windows.

User del -r Kiran ----To delete the user . If -r not used , user will be delete but user data will remain.

Cat /etc/group -- whenever user created , group is also created , and will be stored in this folder.
Cat /etc/shadow contains passwords of user

Wc /etc/passwd -- will give word count of the file 
Line word count char count
Wc-l. Only linecount
Wc -w only wordcount
Wc -c. Only char count

If we want to see only last 3 uses only from /etc/passwd then we can use below commands
Tail -n 3 /etc/passwd  --last 3 lines
Head -n 4 /etc/passwd. --top 4 lines

We need frame wise.. like fir to screen count 

More /etc/passwd -- to see in frame

Creating file commands

Touch f1. -- create empty file
Echo madhu-- print made in terminal
Echo "madhu" > f1.  -- created f1 with madhu inside
Need to append data in the file f1 -- echo "adding append data" >> f1. Double greater than append data.

Linux has vi/nano editor. Like notepad etc in windows.

Vim f1 --- created and open an editor then ,
Press escape I -- to insert data. 

Esc:w to save 
Esc:q to quit

Esc:wq -- save and quit
Esc:q! -- force quit
Right bottom will have which line how much dayta in bytes eg: 5,21

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Day4

/ in linux represents Root directory 
Cd / then ll , we see opt,tmp etc folders . These are imp and created when os is booted
Ll ,ls ,ls -la etc are stored in bin. Executable binaries
Sbin --is admin bin directory it has installation, shutdown commands etc
Boot -- files for boot
Dev. -- device files
Etc -- config files of user, system
Home -- all other directories home dir is /home except root. Root have / or cd root/
Lib,lib64 -- library files 
Media --mounting oendrive etc..
Mnt -- mounting
Opt--- optional dir which have 3rd party things
Proc ,Run,sbin,srv,sys -- let it go for now
Root --- root directory
Tmp --  temprary data
Usr --
Var -- logs storage

-----------------
We practice mostly on opt folder
Cd opt/

To install in windows we download and install
IN Linux, we do it as yum install <softeareto install>
Yum install git --- to install git atlas it ask if it is ok click yes then installed.
Yum install java -- java will get installed.

Cd /opt
Create file and add some data using touch/vi editor etc
 We can use controls x,z in windows similarly it also have shortcuts
Press esc only it goes to command mode.

Gg -- goes to toppling
G - ground
Yy --copy
N-yy -- no of lined to copy
P --paste
Dd --delte
N-dd no of lines to be deleted.
Dw -- delete the word
U -- undo 
There are many, these are basic and must to know

Go to line from which you want to copy then press escape 10yy which means 10 lines from there are copies, similar for delete. To escp. To undo escu.
To enable line number escshiftcoulmn setnu 
To disable line number escshiftcoulmnsetnonu
To search line cntrlf -- escshifr: esc/madhu (searches madhu)escn-- next you can see number and position in the right down 


if you are not admin give sudo su - unmae instead of su -uname
mkdir -p dir1/subdir1/subdir2  --- creates nested folders. -p chcks if dir exists if not creates one.
tree gives the pictorial respresntaion of directory
cp -rp srcpath destinationpath --- copy file from src path to destination path
eg : cp -rp linuxfile a/b/.  - either give name or . indicates linuxfile with same name in provided destination
to move file/cur paste mv f3 a/file3 f3 is now moved to a folder with file3 name
mv f2 file2 -- rename f2 to file2
relative path -- path given from current directory
absolute path -- path given starting from root directory 
eg cp cherry /a/cherry
eg: cp -rp /opt/cherry /opt/a/cherry
cd ../../../ coming out from current is relative
cd /opt etc is abs path
wget <link>   -- to download in linux.
eg: wget tomcat link
unzip tomacat.zip ----to extract <unzip filename>
zip dir1.zip-- to compresst o zip file <zip filename.zip>
zip -r dirnameany.zip dirname

linux mostly use tapearchive tar 
wget linkfor tar.gz

tar -xvf tomcat.tar.gz    --- to unzip
tar -cvf tomcattar.gz appacher

rm -rf dirnameany.zip /rm -rf tomcattar.gz  ---to delete any 

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Day 5
I always use sudo for now else I am getting permission denied error in my system even though I am in root .p

Class notes day wise given by madhu is available in 
https://github.com/devopstraininghub/BATCH18A_B18D_RUNNING_NOTES/blob/main/AWS-LINUX-SHELL/20August.md

Install java and tomcat
Tomcat listen on port 8080. We opened all the port in security group so we can access.
Cd bin
./startup.sh   -- to start the tomcat .
./shutdown.sh  -- to stop the tomcat
To access take the server ip dns name from instance :port 8080 we can access now in on web browser.

This is all called process to check the process command is ps

Ps -ef gives all the process with corresponding command . It has id which is pid.
We can search for process in ps -ef.
Command is ps -ef | grep tomcat  searches for tomact in ps- ef and returns. | is nothing but append the output of ps-ef to grep tomcat is the meaning.
Like search for tomcat in the file and give that output as inout to grep tomcat.

Grep is a filter command
Kill -9 pid forcefully killing the id.

Cat linuxfile grep -I 'madhu' or grep -I 'madhu' linux file -- filter madhu with case ignoring. 

To search in folder and subfolders
Use grep -iR 'madhu'

Cp -rp linuxfile bkp_linuxfile -- always take backup before doing something. ---- good practice 
Diff filea filen tells the difference between files.

Sdiff spots exact lines of where it changes

Server is slowed after deploying like laptop hung in windows we see the task manager and cpu use check
In linux top command is used to check the same. Similar to task bar

Global search in windows there is find command 

Find / -name linuxfile ---- in the / entire root check for name linuxfile and gives the result.
Find / -name linuxfile -type f --- only files 

Find / -name linuxfile -type d --- only directories

Sed is a replace command 

Sed 's/madhu/salman/g' linuxfile --- so here madhu in linuxfile is replcaed with Salman 
If that out put to be stored in a file then  Sed 's/madhu/salman/g' linuxfile > test
With the above commands madhu will be changes to salman but the linuxfile will be having madhu only as we are not giving the change to linuxfile. If we need linux file content also to alter then use below command
Sed -I 's/mashu/salman/g' linuxfile  ---- here s meaning source madhu is the text to be replaced.
Sed -I 's/mashu/salman/2' linuxfile   --- 2 indicates replace the second occurrence
Salman is the one to replace with and g stands for global

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Day 6


Login to was server, create/start the instance. Use ssh to login
Sudo su - 
Hostnamectl set-hostname b18linux<desiredhostname>
Df command -- disk file system - how much disk space is used and how much are is left.
Linux system should be given in files and mount (attach) to some directory. 

In reality df -h / or df -h/opt is usage / and /opt tells space in root an opt folders

Du -sh linuxfile will tell the file size of linuxfile
Yum install tree ---
Du -sh aws/ --- it will give all the folders rise sh stands for sum of all in Human readable form
Du -h aws/ ---- it will give individual size of each

 differnt members will have readwrite and owner persmision to sam file.

-rw- r -- r-- if has read and write

O-owner
G-group.  
O-others

r-read -4
W- write -2
X-execute -1

Owner can have all permission and 
Chmod ownergroupothers linuxfile. It can be given as chmod(4+2+1=7)40 linuxfile
Meaning 7 all permission to owner 4 read to group and 0meaning no permission to others
Cmmod 777 linuxfile
Chmod -R 755 linux.   -R recursively meaning give permission to subfolders also.
Chown madhu:root linuxfile -- changing owner to madhu

Grouped devops --will create a group  and we can see it in /etc/group
Usermod -aG devops ec2-user will add ec2 user to devops group
Play chmod with numbers by giving permissions to group , owner etc.
Chmod 700 jenkins meaning owner has all 3 and other don't have any.
Change group of file
Chow :newsgroup filename
Changes both owner and group of file
Chown newsier:newgroup filename
Change owner and group of multiple files
Chown -R newsier:newgroup file1 file2 file3
------------
Networking commands:
To check if network is working or not in linux
Ping will send data in packets kf all sent then site is working else you will see errors.
Ping connectivity testing
Ping google.com
Ping -c 4 example.com
--
Traceroute google.com

It will tell you where the problem is it can see hop to hop the delayed time indicates see this piece has problem.

Whether the website connectable with network port
Yum install telnet
Telnet www.google.com 443. -- it will show success/failure. 

Clear notes see in git batch 18 running notes
Install java and start tomcat as we know from above commands. Tomcat runs on 8080 port

Netstat will give network statistics. 

Netstat an |greop 8080
If is ts listen meaning someone is using it.
Day 7
Inode (index node)-- is the unique identity given to a file in Linux.  It stores metadata of file. Linux tracks file using node not by file name. 
Metadate -- filetype,size,permision,last modified etc

Command : ll -I or ls -I or ll -I filename
Like how we create desktop shortcuts and all similarly we can do in linux also.
Both let one file "point to" another, like a shortcut — but they behave very differently.

Hard link
Yes
Command --ln target link
Points to the same inode (same data on disk)
If original is deleted link still works — data isn't gone until all hard links are removed
Shown in ll as	a normal file

Soft link

Command -- ln -s target link
Points to the the path of the target file
If original is deleted link breaks ("dangling" link)
Shown in ll as	starts with l, shows link -> target

ln /opt/app/config.yaml /opt/app/config-backup.yaml
ln -s /opt/apache-tomcat-9.0.121 /opt/tomcat
Sample output 
$ ln -s /opt/apache-tomcat-9.0.121 /opt/tomcat
$ ll /opt/tomcat
lrwxrwxrwx 1 root root 28 Aug 26 09:10 /opt/tomcat -> /opt/apache-tomcat-9.0.121
Easy memory trick: hard link → same file, two names. Soft link → a shortcut that can go dangling.

CUT command

cut slices each line by a delimiter and prints just the column(s) you want — great for structured files like /etc/passwd (colon-separated: username:password:UID:GID:description:home:shell).

cut -d ":" -f 1 /etc/passwd
cut -d ":" -f 1,6 /etc/passwd
cut -d ":" -f 1,2,3,4 /etc/passwd
cut -d ":" -f 1-4 /etc/passwd

-d — sets the delimiter (the character that separates columns).
-f — picks the field number(s) to print — a single number, a comma list, or a start-end range.

Sample output
$ cut -d ":" -f 1,6 /etc/passwd
root:/bin/bash
madhu:/bin/bash

Piped version
cat /etc/passwd | cut -d ":" -f 1,6.  --- we can pipe that out put and see in the desired way

Real-time example: Pulling just the username column out of /etc/passwd to get a quick list of every account on a server — a common first step during a user-access audit.

Cut -d " " -f 1 linux.  -- gives output with delimit space we need 1st from from linux file

AWK command
Awk is more customizable and powerful than cut. Below is the command.

Awk -F":" '{print $1 "----->" $6}' /etc/passwd

 awk — a more powerful column tool
awk does the same job as cut but is more flexible — it can combine fields, add custom separators, and do simple text processing.

awk -F":" '{print $1}' /etc/passwd
awk -F":" '{print $1 "=" $6}' /etc/passwd
cat /etc/passwd | awk -F":" '{print $1}'
-F sets the field separator (like cut -d); $1, $6 refer to field 1 and field 6; print builds the output line, and you can glue fields together with any text (like "=" above).

Sample output:

$ awk -F":" '{print $1 "=" $6}' /etc/passwd
root=/bin/bash
madhu=/bin/bash
Easy memory trick: cut → simple slice. awk → slice and reshape.

To copy from one server to another here in linux we use winscp to scp for secure transfer

Within server we can use cp command. But if we need from laptop to server and vice versa we should use winscp or scp.
Ef piranhas file on desktop to linux server.

Scp Command : scp srcpath destinationpath

Scp -I geethkey.pem ec2-server (authentication key username) desktopfilepath servername(publicip)press shift:/temp(folder to which it should be copied)/.(. gives same name or give a desired name )

Basically the second command pattern we use while using ssh.

WINSCP tool

Winscp is a tool that is used to do the scp work only with tool.
Download winscp for windows/Mac OS.
Open click next - once installed.


Hostname take public ip or dns. Portnuber 22 for https always

If have name /passwrd fine. We use the key right so clicked advanced ssh authentication choose the pen key. It only converts to ppl then we are good to use.

Left side is desktop . Right side is server .
Just drag and drop from here to there where it is needed.

Detailed notes available in batch18 running notes.

Ip comes through networks. This is by Nic card(ethernet cable) earlier it is given by cable , now this card is given in cable and through wifi we are seeing.

Ipconfig gives this details.

In linux  command is ifconfig/ ip addr


Tomcat-serverIp:8080 is how we access tomcat. 
In reality case of using YouTube we don't use servers we use dns name which is mapped to youtubeip
Nslookup YouTube.com /Facebook.com etc.
Use nslookup and your publicipname will give the I-address of it.

Wget mcat url used to download tomcat .
We have curl command also to download.

Curl -o tomcat.tar.gz tomcat download link-- tomcat download with tomcat.tar.gz name

If used -O capital O then same download with same name as given in download link.

Mostly curl command  used to test endpoint.
Curl -I https://facebook.com

If we get response code 200 then it is working.
Else it gives errors

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Day 9


Types of linux commands:
sudo su -
cd /opt

id we start any service 
yum install docker -y  ---- install docker
systemctl status docker ----   
systemctl start docker  ----   it will start the service . it will end once syetem restarts, give this again t start.
systemctl enable docker----- will start in booting meaning if system restarted it automatically starts.

in script file
$#   ---- no of arguments
$*  ------- all arguments in one string
$@  --------- all arguments in serapte words
$? ---- to check if laast command success output 0. if  failed : non zero number 

for loop syntax see in b18 running notes
for i in $* or $@;
do 
echo $i;
done
 you will see the differnce of $* and $@
 
 shell variable --name = 'madhu' within the scriptfile 
 local variable --if we have more than one funtions in script
 env variable -- we want to use in session script or any other child process across OS
 env is similar to java env variables.
 already existing en variables we can see in env file.
 env command or  env| grep hostname
command to set env variables  ---- export COURSE ='awsdevops' . now course is avaible across everything in the environment
printenv COURSE alternate to echo COURSE 
unset COURSE -- will remove the course from env
condition and loops
if syntax
if [condition]; then
  echo ""
elif []; then
	echo ""
else
	echo
	//lines of code
fi  
> is -gt
< is -lt   
or need to use (($age> 18))/[$age -gt 18]explore a bit on these also
vaiarable values can be read by $varaiable

loops
for i in (1..100)
do
	echo $i
done	
--- 
while --run till ondition is true
i=1
while ["$i" -le 100];
do
	echo "$i"
	i=$(i+1)
done
---
until loop --run condition is false
i=1
until ["$i" -gt 100];
do
	echo "$i"
	i=$(i+1)
done
---
break statement -- to come out of look
i=1
while ["$i" -le 100];
do
	echo "$i"
	if ["$i" -eq 6] then
	break
	fi
	i=$((i+1))
done
----
continue statement -- will just skip that and rest will be printed  or executed
i=1
while ["$i" -le 100];
do
	echo "$i"
	if ["$i" -eq 6] then
	i=$((i+1))
	continue
	fi
	echo "$i"
	i=$((i+1))
done
details syntax is avaiable in day9 running notes.practice this with loops and coinditions
-----------------------------------------------------------------------------------------------
Day 10 

arthimatic operations for number are common . list is availble in notes+,-,*,%,/,** double star for power 2***3 =8
you can have these also in a script file.
logical/boolean and string operators
-z  to check string is empty?
-n string is not empty
!= not equal
 && lofical and 
 || logical OR
 ! logical not
file opearots
-f file exists, -d directory exists, -e file or dir exists, -r readable, -w writable, -x executable, -s file not empty.
increment-decrement and assignment operators
set -u --- fail if you refernce a variable that is not set. eg $env is variable but as a typo $envv is called out in script.
set -o pepefail -- makes while pipeline fail is any |commands are there given this if one pipe failes then all fails.
set -euo -----we can declare this also for all things to executecat fb.loof  grep "error"

in is used to check in a set of values.
for user in (all users use cut command to get the user info.
path variable tells linux where to search.
eg: path  =/usr/binpath

functions
syntax
function function_name{
command 1
command 2
}
or

function function_name()
{
command 1
command 2
}

 if we need to execute same code  repetead of more than one time then we can use funtions
examples find in running notes 
cpu usuage command ==top
top Bn1| grep %Cpu(s)
real time examples of these are listed with commands in notes.
Bn1  meaning batch snap of that.
free -m gives space
----------------------------------------------
day 11 interview questins discussion
rea the b18 notes provided and check the task, pdf and questions notes. then you will get a grip 




















