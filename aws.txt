AWS DAY1

same as linux aws . creating the aws free tire account and exploring ec2

DAY2 EC2+ EC2 +EBS +EBS snapshots
Was has two IP's one is public  for public people and in house  access  it with private IP.
Touch 
Whom
Sudo su -    to work as root use
Login through either ssh or web browser to connect ec2 instance.

Consider we have 2 server in two different regions,
 Can we add extra storage to server. Yes it is called volume /block volume. Like how we add pen drive /external harddsik to computer smilarly.
When we create the ec2 instance  we will have  some v=block volume (eg:8GB)
If we need to add any extra to it go to elastic block store in the aws UI

In terminal post ssh login 
Command -lsblk
Now we have to most (add) the ebs(elastic block store) to our ec2.
Ebs tp be attached to be created in the same availability zone of ec2 server.

Steps:

Volume settings:
Type -gps sp3
Gib -take 1 /2 for now the more he more bill
Availity zone - same zone as server 

Tags-  give a name like easy to attach -- us-east1-a in class.

To attach this volume to any instance. Right click on volume -- attach - add the instance details. Select the instance you want to add , in device name add /dev/sdf or any which is available. Now vol1 will be changed status to in -use

Now if we to lsblk, we can see the attached volume. But in order to use it we have to mount it .
Use df -H (to see the file ssystem)and you cannot see this details in there without mounting(attaching).
We need to create file system structure to the extra 1gb then we can use it.
Steps:

Mkfs -t ext4 /dev/nvme1n1. (Make filesystem ext4 is a file system type -t type  /dev/nve1n1 is volume name)
Mkdir madhu.
Mount dev/nvme1n1 madhu.  -- it will mount the voulme(ebs) to madhu folder
Now if we do df -Th it will be showed in file type and now we can use it through madhu.


Cd madhu
Touch chair Venky balayya etc files (consider we added 1 gn files will be stored to 1gb)

Now server2 which is in another zone 
Then we can detach it from server1 here by right clicking on volume and attach to server2 or we can do one more,
Take the backup of voulem1 which is called snapshot and attach it it server2. Now both will have the same .
Bought -- if I made changes n volume 1 after backup , will it reflect automatically in snapshot or I need to take the fish backup if something change in v1 and to be reflected to v2. 
mkdir madhu
mount /dev/nvme1n1 /madhu
it is now applyting the created block space to madhu directories
now create files.folders etc to madhu.. this will be present in madhu directories
we can take the backup of existing which we call call snapshot and attach to another server.

rightclick on snapshot, create snapshot in details sgive a name , in tags give a name and backup will be gone s3,
we can also create volume from snapshot. right click on snapshot, create voulme and do the voulme steps here, vnow voulme1 and voulme2 are back up to each other. add this voulme 2 to another server instance after creating one. and follow the same steps mount it to server and you can see voulme1 created files in voulme2 whi h is server2 also.
create a voulme in mumbai and create asnapshot and attach it to another server in another region.

so this backup concept, with EBS if we delete abc file in voulme1server1.
snapshot volume2 is attached wont be deleted .
EBS will not ahve that concept.

we have another concept where we can have a centralized space, once any modificatins done, then it will reflect other.
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Day3 | EFS vs EBS | NFS & Mount Targets
we need to have shared space which can accessed to all 3 servers. modificatin ic common spcae results in changes in all 3 servers.
this can be solved by using EFS(ELastic File Storage)

In linux we have shared mounts (NFS)and they will share. same pattern in AMazon is reffered as EFS

Go to AWS -->Amaozn EFS->filesyatem->create.
this is region specific and can also be shared on single availability zone.
give a name, keep vpc as is,click customize-> here check either regional or availability zone specific  use regional for practice so we can use across multiple zones in same region.
life cycle mangament will discuss later
classifications  based on used frequenct.
standard-- mostly used
infrequent -- lightly used
archive --- rarely used
in network access
now, choose the mount tagets 
choose the zones in which we are trying to mount this efs.
select security group available in each zone.

file syatem policy is like permission we can give for now leave it blank. create.


now create ec2 instance servers in two availablity zones of same region.

now we need to mount the EFS to the created servers using bewlo steps.
click on ttach on the right side on efs,
copy the commands presnet , opne cmd and login to aws via ssh for bothseveres.
hostnamectl set-hostname server1/server2 as required. 
sudo su -   ---> to go to root user
copy the command given in attach and paste in server1 and serve2
we will get error as efs is not avaiable so create mkdir efs.
yum install amazon-efs-utils -y   -- it should be there before trying to use the copy command from attach else will get errors.
df -Th  now we can see file syatem mount to /root/efs
if we are in some abc folder the abc/efs wil come better do in root to avoid clumsynes.
now play around by creating some files and folders . deleting some files and folders in both server1 and server2. and this is reflected in both the severs now both the severs acts as actual backup of each server.

when to use EBS and EFS
EFS is a bit slow as it needs network and etc..
EBS(eg pendrive) is less costly compare to EFS(eg google drive).
realtime example you can find the running notes of that day in git hib of mindcircuit b18 running notes
++++++++++++++++++++++++++++++++++++++++++++++++++++++
Day 4 | AWS Load Balancer|httpd web server setup

why we need load balancer?
if one server gets down, we need other server  to send the desired  output.
in aws we have ELB (elastic load balancer), which distributes the load to differnt target systems.
log in aws consile, left panel we can see loadbalancers.
1st need to leran how to configure webserver.
install httpd package which allows webserver things.
yum install pkg_name -y  -- to install the service package
systemstl start httpd   -- to start the service
systemsctl status /stop/restart  httpd --- to check the staus of service
systemctl enable httpd
code /vat/www/html/index.html -- example :sample html sevice which we are using .
here create instance  and choose avaibility zone and other etc.. and create minumon 2 no of instances(choose from right ).
rename fb1 and fb2. connet through ssh to both servers in local.
sudo su - ==>to go to root in both
install http package yum install httpd -y
start it systemctl start httpd
systemsctl status /stop/restart  httpd --- to check the staus of service
systemctl enable httpd

copy its public ip/dns:80 in browser  you will be able to access.
now go to  cd/var/ww/html/
create index.html here and write anythig example for now echo "<h1>i am fbserver</h1>" > index.html
some html syntax basically
to code good  go to browser open facebook .com right click code copy and page in file using vim index.html paste it here with insert mode.
take any sample page.
you can aslso ask gpt to create a page for you and take the code and paste it here in yout index.html page.

difference between aws ec2 and webserver.
ec2 is a emplty server. we can fill it with anything now we are using it for webserver. we can use it as db server, middleware server etc.
------------------
In Load balancers
create target group .
go to create target group ->name it b18 targetgroup-> http port -> health checks- index.html will have the data , so give this in health check. give a timeout-> 3 sec for example.
unhealthy threshold: meaning if healthcheck fails continuousky give n(eg:2) , then we call it unhealthy node meaning dont sned request here.
also healthy threshold checking n(eg:2) nof of times continous success , then consider it as healthy node.
in how much timeintervak it should check is interval like 5 sec or 30 sec etc.
success code: set it to 200.
register target add it here the 2/more  severes created. click create target group.
--------------
Load balancer
 we have differnt types of load balancer.
 for http/https we called use application layer load balancer. it is also called layer7 load balancer. http protocal is in 7th layer in network architecture so called layer7 load balancer.
 
click create-- give a name --> intenet(over the whole network) /internal(with in college/company)
network mapping:
where these are avaiable -- pick where the servers are avaialble the same region/zones accordingly.
add the target group  we created here. create . once done /in review also we can see whole configurations.
you will see th loadbalancer DNS co[py and paste it in sever , we can see now.
we cannot idenify which server it is goinf at the moment right, just for trouble shooting , make a small change like text/ some note like server1/server2in one of the servers and now load the pages(refresh) we can see from which server it is coming.
create multiple servers in multiple regions and play around create target group and check it.

purposely  stop any server using systemctl stop server2. now 1st time itgives  502 bad gateway then it omes from server1 which is avaiable. in raltime also it is same.
deregister the target from AWS UI in target groups  now slowy requests will be moved fto other server it is called draining.
consider we down the avaialble zone itselft it gives 503 service avaiable eventhough service is avaialable.

consider the site is giving offer and we have more  data coming., these hots we can see in requests in monitor sections.
so if there is a slae we increase the server based on architecture design and etc.
 
after practice terminate everything, load balancer, target group and servers,ebs ,efs everything we did.
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Day5 AWS-AMI-Launch Template

AMI -- Amazon Machine Image-- it gives us a server/ec2 image which we can nothing buy givings computer feel but in cloud.
Login to AWS
Create a EC2 - use ssh login /login
Sudo su -  go to root 
Cd /opt -- opt is an optional folder 
Yum install https -- install it 
Systemctl enable http/systemctl start https --- start the server
Create a html page using get/own keep the html code inside cd /var/ww/html/ index.html then start it and use that ip:80 in browser we can use it.
Now if asked to have such 100 pages, we cannot manually go eveytime and and do, instead we should a create a custom AMI to automate this steps and the required code we can do changes in index.html if any.
Data,package and service packed in a server can be created as custom AMI with OS and data+package+service with below steps:

Go to instance -> right click->create image ->give a name mc-ami
Give a tag, volume if any needed, also note image creation process crates a snapshot automatically as instance is now acts as a backup.

Now go to instance -- now it will reflect our custom AMI, so choose that from MY-AMI. Do the same as wd create a server. Choose instance no >2/more as per ask.
 
Connect it through ssh/webclient. Here in copy we will see appt change it to ec2-user as ourpem works for ec-user. Then later login to sudo su - root user.

Paramaters required to create instance
Name,ami,key-pair, network-security group, instance type,voulme.

Now we customized AMI, instead if we want all the above parameters to come without giving manual , we need to use create launch template.

EC2 launch template:
 
 crete launch template - give a name(mc-lt) and we can add version to it as v1, v2 etc.., give a tag and select mc-ami which is custom, t3- and in kep pair choose geethkey and storage  etc as per the requirement, click create launch template.
 
 view launch instane and you can see , this way everything is a ready to cook kust give no of instance needed.
 try adding these serveres if needed  in load balancer.
 create target group then add this to ALB, now use the last class notes to create load balancer. use this ip in browser the dns name, it is disctribued across 4 servers in target group for eg. load balancer does not need port 80. if you ant to see which server  it using  just change some data in one of the servers it is to debug only. in realtime all serveres wil be identical.
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Day 6  ASG |SNS
 now we have 10 instance avaiable with same base project. example, we now there is more demand  we need to increase the server no, when load decread servers need to be dropped down. this can bea chieved by autoscalling.
 minimum capacity is 1. max capacity  is 10 (min and max is decided by architect).desired count is also there which aws will fix as below.
 aws will check for  current no if avaible services and desired count if current =2 desired count is 3. if load increases it will add 3 more serveres which is current +desired. (2+3=5). if current is 5 and desires is now 1 then current-desired (5-1 =4) will be server count. this way autoscalling works.
 Load can be anything. server cpu % , ram usaugae, no of req count on load balancer etc.. we can have a threshold count on which autoscale occurs.
 
 we need to prepare a launch templated prior and  based on that it will autoscale. also we need a sms/mail noditication eveerytime autoscale occur.  this service is called SNS. we cover this topics and create them as below.

 ec2- launch template- we can create new/ modify the existing as per neeed. dont add avaiablity zone in launch template. autoscale will deicde based on demand.
  
 EC2- autoscaling
  give a name , mc_as, then next network -> new instance should be in multiple avaiability zone  to avoid down time.
  availability zone disctribution -new
  balanced best effort --chooses suitable as per it's intelligence. balanced only-- will try to create in same az if failed , reservation the blance- in other az if failed . choose  balanced best effort.
  load balancer all keep default.(no load balancer)
  health checks as we new if health check fails it will change/create new instance(server).
  scaling min 1 max 10 desired capacity 2 in the intial later autoscale will decide.
  choose autoscaling policy as per need like avd cpu utilization( to check keep 25%), instance warmup- 300 sec example is a booting/helath check time. 
  scaleIn -- delete the old server when not needed.
  scale up-   adding on load. by checking the box, only scaleup will occur, no scale in.
  instance maintaince policy -- keep none for now -- read about these in details.
  keep the rest as is for now.
   add noticication(simple notification service) -- send a  notification to --   and email name -- provide as per need, select the events below all.
   give a tag if you wnated. now create scalling . intially it will create 2 which is our desired count ans AZ will be as per it's choice.
once done,  isf sns chosen, mail will come, subsribe on/ verify it for 1st time.
-----------------

To test autoscaling working or not, follow below steps.
In windows we can do it as below.
 open cmd/gitbash..  type yes, it will come yyyyyy which increase the cpu load and keep on doing it with multiple gitbash and yes.. will increase the cpu load.
/dev/null is called black file -- as it observes everything it takes, do yes > /dev/null this 
we can do the same in linux as below
go to ecs .. connect  as ec2-user in  webbrowser
top  -- to check cpu usause and all  
yes >/dev/null &   --- command to load cpu
do this in 2 servers created. check Top again you can see load increased.
now you can see new servers created nad desired capacity increased after some time. and all the servers will have the browser dats.
as we are using free/community huilder account , 10 cant be created  as default is 8. so if we need more serveres, mail aws we need which is not needed now. just copy the error and ask gpt for more details .

to teminate thee kill -9 pid --- now kill all the yes magic for testing in all serves. now cpu utilization decreaes and slowly servers count decrease which is again autoscaling.
cpu count for each is provided in the launch instance time in t3.micro we have *2.
activity screenshot/desired count screenshot paste it in group.
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Day7 VPC
earlier we have physical server with os and all later virtualization came into pictur. here also we have server, memory etc. which is Vmware, with viryalization we can divide in multiple segments(machines) and each machine can have 10 when in total we have 100.
clean notes avaiable in vpc notes , please go through :P
virtual machine physical servers are some where in datacenter.
cloud should gauarentee data of each company is not specified across . so we in aws we have concept of vpc, virtual private cloud(VPC). which is a shared responsiblity. aws will not allow communication with serves, if comapny wants  they can establish the communciation between servers.
CIDR

In a network a resounce identity is Ip address. ipaddress is provided via CIDR.
10.81.0.0/16 eg : these are not numbers these are octals.
One number represents 1 byte. Above is 4 bytes. 16 is bits here.
1 byte- 8 bits (01010101)
4 bytes - 4*8 -32 

16 meaning 1st 16 is fixed. meaning the ip range would be as below
 10.81.0.0
 10.81.0.1
 10.81.0.2
 10.81.0.3 ................................ 10.81.0.255

10.81.0.0 /8
10.81.0.0/16
10.81.0.0/32

 likewise we have many ip ranges.. with one CIDR defining.256*256 =65.536 65k 
Likewise we can call each set  and call it subnet. This subnet we have public and private subnets 2 in each.

1. We will create a vpc.
2. Internet gateway
3. Subnets 4. (2 will be enough, just for clear understanding creating 4)
4. Route table
5. Configure

Practical
 Go to aws console--> in search VPC- do not touch default which is provided by AWS , create our own.
Give a name, ipv4 cider block - five a range(10.81.0.0)
Go to internet gateway from left panel -?create. Right click attach to our vpc.
Subnets -- each avaiablaity has a subnet for default provided by aws.  
For our we create subnets in two AZ and Each AZ has 2 subnet 1 private and one public for practice.
Create subnet-> keep adding subnet count in settings. Give the a name PUB_SN-1A,Pvt_SN-IB,2A,2B. Choose the AZ.
Give a range-> 10.81.1.0/24, 10.81.2.0/24,10.81.3.0/24 ,10.81.4.0/24 
There will be default routings , create one for our won.
Route table-->create- give a name,
10,.81.0.0/0 -- meaning any ip there is no fixed.
By default all subnets are private. When subnet are added I. Public subnet it is called public subnet.(routable association).
Route tables- add public subnet we want in explicit subnet associations, now it is public subnet.

Now go to EC2-> create instance-> in network settings-> don't use default, instead choose the our own vpc with public subnet here and enable auto assign ip-> enabled , create a security group as the old SG we are using in for another VPC. When VPC changes it will be different SG, select key pair and done. Similarly create one instance for private also . Here auto assign ip will be default disable, just to show us enable. 
Now  connect through ssh/web browser , but we can connect to public but we wont be able to private.
Will get connection timeout error for private.
To keep data inside private how?
Within the network (VPC) -> we can connect to private subnet and playground.
So, connect to public and from there try accessing private.
Hostnamectl set-hostname pubsrv. Connect in public ping private  it will success, 
Now copy the ssh commands we use to connect but default it will be public ip change it to private and connect.
It will throw error as key in in laptop , not server. Now copy key from laptop to server.using winscp/ copy commands.
Ll
Vim key.pem
Copy the content from key in laptop and paste in this file. Give commode permission to the key copied and then use the connect command use private vpnm as it is within vpc.
After practice delete, instance and delete VPC vpc deletion delete all the RT, subnet etc created as part of this. Then double check to avoid any data loss.
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 
Day 8
to enable communication between server's we have peering.
create vpc- select vpc and more. 10.81.0.0/16
1 AZ, 1 public subnet(connect to route table), and a internet gateway,customizing the ip is also possible here.
subnet choose 10.81.1.0/24.
similarly create one more VPC. one for facebook and one for insta VPC.
by default facebook and insta have no communicatoin. now establish peering like bluetooth connect.
now create ec2  instance with fb vpc of insta and facebook and connect to it by ssh /web browser.in vpc, go to vpc peering. give a name.
selct local peer gb to insta i.t from and to . create. if we have to connect connectin  from differnt account that is also possible , juust give the account  details with it.
go to RT-- add route-- if insta sends dats-- facebook should handle. and similarly do in insta also, if facebook sends data, insta handle. and save.
create a vpc in mumbai region- cretae a ec2 instance whatsapp. and choose the created vpc, sg , create keypair and save it.

key-pair is region specific and SG is vpc sepecific. connect to this whatsapp ec2.
lets establish peeriing between fb and whatsapp inter regional.
this time chose another region, copy vpc id accepter as mumbai vpc . requester is facebook from noth vergina.
establish routes of fb and whatsapp, in fb -? add new route4 from whatsapp fb will hadnle and in whatsapp add fb cidr and save. now for checking use ping pvtip of fb , insta and whatsapp in each other and check. now we can connect from one ec2 to another ec2 with above steps.
peering is not trasitive, it.e a->b b->c this doesnot mean a->c..
copy public ipof fb,whatsapp,and insta.
in fb installl(yum) httpd systemctl start httpd, in vim /car/www/html index.html and write welcome fb.
copy the publicip:port, now it is accessable to ouside world.stop the fb server. the stopped servrs dont have public ip.now start again, now public ip differs.and for accessing use the new ip/port.
so everytime a ip changes, we cannot go and change it to all users. if we need a fixed public ip which is static we need to pay aws for static(elastic IP). to go there, go to elastic IP's network and secirity.
create a ip, region , give a nameinstance choose facebook if wanted.. now a fixed ip is provided to users. it wont be changing after stop.
so, give elastic ip and port to it and we can use/stop/start it anytime, no change will be added.

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Day 9 AWS-SG-NACL-ENI

Delete all the vpc peering created, ec2, peering , route tables and vpc and everything in all regions.
Never ever touch the default vpc provided any region.
Create a ec2 instance, default vpc has auto assign public ip is available.use sg and all which re old/create new as per you choice.
Click on advanced details in network, we use user data. We install https package and enable/start it and in index.html we write our code.
We can write these commands in user data.
Yum install https -y
Echo "this is sample " > /var/www/html/index.html
Systemctl start httpd
Systemctl enable httpd

Here only the fb server intiates.
Server's ultimate defense is SG. So port enabled in SG, then only public can access this port. This is called incoming traffic.
Data sent from server, which is this is sample  is outbound traffic.
-----
Instead of creating it while instance creation, we can also create separately in security groups.

Give a name, description.
Inbound rules-- instead of giving all traffic, we give type ssh, https, mysql and any particular Ip, like DB will route through a specific Ip only these all should be configured.

For outbound there are no rules, it's like meeting CM needs protocol , but CM can meet you anytime CM wants.
Debug ig app is not working
1. Check app is running/not using ping. 
2. Check index.html has correct and correctly mounted.
2. Give top command to check CPU Usage.
3. Network , check inbound rule, check http/https, ssh enabled. If the ip through which you are accessing is enabled or not in inbound.
 Do changes needed here if any, then we can easily trouble shoot and solve the error.
Security group is stateful. Inbound rule provided, no need to give the outbound rule. Meaning the same path is allowed for both inbound and outbound.
The security mechanism at server level is called Security Group.
------
NACL
 Consider we have 3 servers, 2 by devops and 3 by user and user allowed all ports now how we can control it. User server will be used by many detest etc.. 
The security mechanism at subnet level is called NACL.
NACL is stateless , here inbound and outbound traffic is different, lowest number gets highest priority.
Click on vour vpc->network ACLs, click default, open it will have 6 subnets.
By default it allows all, so now we can add deny this port also here. -> edit inbound give a lowest no  , paste the Ip and choose deny. Make sure it 100 ia having all traffic, your custom will have number <100 , else it wont be applied. As lower number has greater priority.
Play around this by allow/deny multiple protocol and ip , will be useful for practice.

Network has multiple servers which is uniquely identified with ip address.
Ip's attached to servers in a comment which is called NIC Card.(network interface). AWS calls it ENI. Elastic Network Interface(ENI)
In Windows Ipconfig will give wifi /cable connected.
So all the SG, ip everything is attached to NIC card.
A server have multiple IP(public,provate.SG and etc). Some applications  demand multiple IP's. In such cases we need to attach one more network interface.
In Linux ifconfig will have the features .
EC2->Netwrok interfaces-> give a name-> no need to set any type ENA-> subnet chose where server is there. 
SG - our won choice SG, create network interface.
Actions->attach->chose the vpc and instance. It will have extra IP.
Check ifconfig we can see extra IP for this server. No we can use the app in the newly given Ip also.
-------
NAT Gateway, private server does not have internet access, so to enable Internet in private, we need NAT gateway. Will discuss about these in later classes.
 
 
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Day 10 AWS MiniProject

private subnet cant be accessed directly anytime, so need a public subnet which is dummy called baston/jump subnet, which should be connected with private via route tables. 
private subnet has no intenet  by defauly. if has to be established it can be done by NAT Gateway.
NAt Gateway will mask the provate serverIp and give a static ip to public by ElasticIp.
load balancers will be placed in public subnet and target servers are placed in private.
so consider fb is hosted on private server which is in targetgroup and load balancer is kept in public and it should be handled by autoscaling as per request coming/load giving.
the aws documentaion for aws miniproject is avaialble in aws documentation,Link is provided in batch18 running notes.

login aws console->create VPC(vpc and more), cidir block 10.81.0.0/16
follow the diagram as mentioned in document and create 
a AZ NAt Gateway zonal and 1 per AZ,2 public and 2 private each in AZ. 
NAT is always created in public subnet only.
create targetgroup for load balancer, currently it will be with zero targets.
attach it to loadbalancer with our own vpc and avaiablity zones where vpc is there. and load balancer should be always there in public subnet.
create Security group as it is 1st time in the vpc, all traffic, froward to freshly created target group.once load balancer is up and running, check the dns in server but there wont be any for now.
now go for autoscaling, for this we need a launch template. go to ec2->launch template-> aws linux crete one with all the requirements. network -> choose out vpc and dont choose AZ, as we need it to be created by aws inteligence.
in advanced userdata, add  your server related page like inde.html in cd /var/www/html
in gpt ask, provice userdata script  to deploy httpd in aws linux server  in index.html simply say "hi from geethika in it ". just some sample data. tweek it to add server hostname in it just to know from whichs server it is coming , as load balancer is attached, it might come from multiple severs.
now got o autoscalinggroup,  chose the above launch template,  in network our vpc, chose in az and subnets private subnets(2).
in load balancer, 2nd option , give our load balancer, chose the derired, minumum and etc , refer ASG notes and create a SNS also,and create.
in load balancer now, give the DNS , then it will give the data which we provided in userData.
if any issue comes in data, or server not working as expected, then we need to debug.

in ec2 lets create a jump/baston server which is for public.
go to ec2-> aws linux, vpc should be ours , subnet take anyone public and create  with giving security group and all.
now connect to jump server,using either ssh /web browser.
sudo su -,hostname to jump and ping google , it will come as public has access to internet.now give the pem key do vim geetkey.pem and copy paste the key from laptop.In this server, and gvie chmod permision here and  copy the private server sudo and paste in public server console now we can connect to private through public as there is now conenctivity between them.
now in private server conenct and check and try ping google.com , it will have access to internet as we have NAT gateway attached to it.
it can access the network and others , but through internet we can access the private server .

to have more clairty go to route table of private server, remove nat gaeway route, then pvt server cannot access internet. and add it again for recheck, will be able to access again internet.
we can downlaod git, appache and all other necessary servers. in real time, we do the same.
after practice make sure, you delete everything you practice.
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 Day11 IAM 
theory find in inb18running notes,you cant give uname pwd to everyuser joins, so we set authenticationa nd wutherization limits to user based on thier roles. 
generally users with same/similar roles are grouped and that group will be given specific permision.

login to condole ->IAM Users -- create user.give  ausername and password, check it needed to changed afgter sometime or conntue with the same.
set permissions- for now dont give any permisiions, review and create. iam user created.
a link will come in console signin links, in sign in user IAM login and give the username and password provided to login.
if you check this profile, as no permision is given , user is denied from everything. to give permission , in set permission atach dpolicy eg: admin right or anyother as required.now user will be able to launch instance etc.
 we can also send outlook mail with the given uname ,password etc.. which is realtime .
 we cna group all developers, testes etc in a group,
 IAM -user groups- select the users and set the permisions eg:admin as per ask,created user group.
 user can be part of multiple groups.User inherit permission from all the groups he is part of. (A union B).
 the policy will have a json file which holds all this permission data.
 action:  * s3:*/* etc meaning s3 all buckes etc
 effect: allow
 resource: * all the resources allowed, so simply chise the policy from ui and json is created automatically.
 
 eg: ramani should have ec2  starta nd stop access only, then how we can do?
 attach policy directly->if no default polisy there, then we can have custom policy for requirement like above as below.
 
IAM->policy->create-> ec2-, action allowed-> start , stop , describe -> resouce , instance we can give region and instance name, give a name, and create instance.
copy the json created, we can also do it by changing the json also. go thorugh notes you will get it.
now go to user/group-choose custom policy and give the custom policy you created.
 to do it through json-> choose json instaed of visual,, in right side chose the action on ec2: start , stop. add resource ec2 instance region: id add resouce and one policy ready.
 3rd way
 aws policy generator : i am policy ytpe, effect:allow.deny.
 aws ec2->actions,add resouce arn here and add statement, policy created.

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Day 12

IAM USER- has built-in  and custom-made policies also. It has action, effect, andresource is ARN .
IAM has roles -- IAM roles will be discussed later. 
IAM is global as this deal with user, globally it should have same.
VPC ,KEY-Pair, EBS are region-specific.

Storage services
EBS,EFS likewise we have Simple Storage Service (S3).
It is highly available as it is available across multiple regions.
S3 is highly available, scalable and cost-effective.
Probability of missed object stored in s3 is 0.000000001% , so object is never lost.
It has many classifications.
Data used regularly, temporarily, or rarely can be stored in different storage services.

Go to console->S3-> data/object stored in space is called bucket.
Go through the video provided above . Go through he connect and aws docs also for better understanding.

S3 is regional service and naming should be globally unique.
S3->buckets-> create bucket.
General purpose -> choose a region-> give a bucket name-> give a tag and create a bucket.
 Upload anything file.folders or any. We can also drag and drop by clicking on upload.
Object cant be directly accessed.it should be given access.
Once created we get ARN->amazon resource name , s3 URI and tag if any and object url which we use to access it in browser.
Bucker level, go to permission remove all block public access, now gate is open at bucket level.

In object ownership edit and  enable all enabled. And ownership bucket owner preferred.
Now to to object and see edit access control list and give the access to whomsoever needed.
We can also enable versioning.
Go to properties and edit bucket versioning and enable.
Now upload the abc file with changes in 2 or more versions, if enabled we can see as v1 and v2. Else it existing  data will be overrides.
To see the older versions, enable show versions. And you can see all the versions of the file.
To deletion, select, delete-> we can undo the deletion if versions enabled. Deletion marker an versionId is created for it. If want to retrieve, click on delete marker and delete now, it is available.
To delete permanently, type permanently delete it is deleted all for good. If versioning level it is deleted It is gone for good. 
-----
We can host static website on s3 also like we do in EC2 and s3 is cheaper compare to S3.

Go to s3- buckets-static website hosting-> edit enable-> host a static website.
Index document
index.html.  --- our page content.
error.html.  ---  page when's one error occurs. And save it.
Dynamic website meaning--content changes every time eg siwggy displays menu based on our previous order etc.
Static website-- same html page for all time and anybody-no matter how many times and to everybody.
It will give a url through which we can access our page. Take astatic website code from get or give some simple data for your index.html  and some error message for error.html.
Go to s3, add these two files in s3. Now provide access --> go to s3 buckets. Bucket policy---edit
Aws policy generator-> bucket policy, prinicple * action a* and bucket name take the arn of our bucket, generate policy. Copy the content and in resource : arn/* add /* extra and paste in bucket policy. Save changes. Try accusing the page with the url provided above, will be able to access the status website through s3.



 We have storage classes in s3 standard meaning regular usage.




